<header id="header" class="header fixed top-0 left-0 z-50">

    <nav class="navbar navbar-expand-lg bg-success h-100" style="height: fit-content;--bs-bg-opacity: .3;">
      <!-- 左端ロゴ -->
      <%= link_to root_path, class: 'header-logo ms-4 py-3' do %>
        たびログ
      <% end %>
      <!-- トグルボタンは右端 -->
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarTogglerDemo03" aria-controls="navbarTogglerDemo03" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse me-5" id="navbarTogglerDemo03">
        <ul class="navbar-nav mb-2 mb-lg-0 ms-auto ">
          <!-- session -->
          <ul class="nav-item_before dropdown before-login">
            <a class="nav-link  text-dark d-flex" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <span><i class="bi bi-book"></i></span>
              <p>ログイン</p>
            </a>
            <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
              <li>
                <%= link_to 'ログイン', new_user_session_path, class: 'dropdown-item' %>
              </li>
              <li>
                <%= link_to t('devise.shared.links.sign_up'), new_user_registration_path, class: 'dropdown-item' %>
              </li>
            </ul>
          </ul>

          <li class="nav-item_before ms-3">
            <%= link_to planners_path, class: 'nav-link d-flex' do %>
              <span><i class="bi bi-book"></i></span>
              <p>アプリの説明</p>
            <% end %>
          </li>
        </ul>
      </div>
    </nav>
</header>